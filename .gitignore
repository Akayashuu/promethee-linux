# Build output — contains Promethee's own code, which must never be committed
# or redistributed (PolyForm Noncommercial).
/dist/

# Local copies of the source bundle, wherever they land.
/.source/
/.backup/

node_modules/
*.log
